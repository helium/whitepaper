sudo: required

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y texlive-full texlive-latex-extra texlive-latex-recommended
  - sudo apt-get install -y latexmk

script:
  make build
